# Pest Control ERP

Production-oriented MERN web platform for one pest-control company with multiple branches.

## Setup

1. Copy `server/.env.example` to `server/.env` and set secure values.
2. Run `npm run install:all`.
3. Run `npm run seed:owner --prefix server`.
4. Run `npm run dev`.
