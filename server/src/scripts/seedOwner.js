import 'dotenv/config';import { connectDatabase } from '../config/db.js';import { Company } from '../models/Company.js';import { Branch } from '../models/Branch.js';import { User } from '../models/User.js';import { ROLES } from '../constants/roles.js';import mongoose from 'mongoose';
async function seed(){await connectDatabase();if(await User.findOne({email:process.env.OWNER_EMAIL?.toLowerCase()}))throw new Error('Owner already exists');const company=await Company.create({name:process.env.COMPANY_NAME||'Tech House Pest Control'});const branch=await Branch.create({companyId:company._id,name:process.env.DEFAULT_BRANCH_NAME||'Head Office',code:'HO'});await User.create({companyId:company._id,branchId:branch._id,name:process.env.OWNER_NAME,email:process.env.OWNER_EMAIL,role:ROLES.OWNER,passwordHash:await User.hashPassword(process.env.OWNER_PASSWORD)});console.info(`Created owner ${process.env.OWNER_EMAIL}`)}seed().finally(()=>mongoose.disconnect());
